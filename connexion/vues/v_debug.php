<?php
  // $mdp=$_REQUEST['mdp'];
  // $mdp = hash('sha256', $mdp);
  // $mdp = hash('sha256', "saucisse".substr($mdp, 0, 32)."halloween".substr($mdp, 32, 32)."chat");
  // echo $mdp;

  echo ($utilisateur['uId']."<br>".$utilisateur['gId']."<br>".$utilisateur['uEtat']."<br>".$utilisateur['uActif']);
 ?>
